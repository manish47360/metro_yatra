class AlertDestination{
  String stationName;
  String stationCode;
  double latitude;
  double longitude;

  AlertDestination(
      this.stationName, this.stationCode, this.latitude, this.longitude);
}